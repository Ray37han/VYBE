# Nixpacks configuration for Railway
[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["cd server && npm ci --production"]

[phases.build]
cmds = ["echo 'No build step needed'"]

[start]
cmd = "cd server && npm start"
